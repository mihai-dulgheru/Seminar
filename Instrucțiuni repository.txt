1. git init
2. creare fișier .gitignore
3. adăugare node_modules/ în fișierul .gitignore
4. git add .gitignore
5. git commit -m "added .gitignore"
6. git config --global user.email "dulgherumihai19@stud.ase.ro"
7. git config --global user.name "Dulgheru Mihai-Nicolae"
8. git commit -m "added .gitignore"
9. git branch -M main
10. git remote add origin https://github.com/mihai-dulgheru/webtech-s01.git
11. git push -u origin main

error: failed to push some refs to 'https://github.com/mihai-dulgheru/webtech-s01.git'
hint: Updates were rejected because the tip of your current branch is behind
hint: its remote counterpart. Integrate the remote changes (e.g.
hint: 'git pull ...') before pushing again.
hint: See the 'Note about fast-forwards' in 'git push --help' for details. ->
	$ git pull origin master //run this first, then
	$ git push -f origin master

12. adăugare .codesandbox/ la .gitignore
13. git commit -a -m "added .codesandbox/ to .gitignore"
14. git add .
15. git commit -a -m "initial source commit"
16. git push / git push --set-upstream origin main

git add hello.js
git commit -m "added hello.js"
git push

Install express:
	$ npm i express
